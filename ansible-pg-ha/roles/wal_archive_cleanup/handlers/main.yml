---
- name: Restart pg_autoctl after legacy WAL cleanup
  ansible.builtin.systemd:
    name: "{{ pg_autoctl_service_name }}"
    state: restarted

- name: Reload systemd after legacy WAL cleanup
  ansible.builtin.systemd:
    daemon_reload: true
